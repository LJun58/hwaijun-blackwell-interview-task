import { Button } from "@mui/material";

export default function RegisterButton() {
  return (
    <Button
      variant="contained"
      color="warning"
      sx={{ fontWeight: 600 }}
      //onClick={() => {}}
    >
      Register Now
    </Button>
  );
}
